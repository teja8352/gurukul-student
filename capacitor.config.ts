import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gurukul.vidyarthi',
  appName: 'Gurukul Vidyarthi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
